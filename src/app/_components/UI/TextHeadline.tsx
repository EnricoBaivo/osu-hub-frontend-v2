
const TextHeadline = ({ children }: { children: React.ReactNode }) => (
  <div className=" uppercase tracking-wide text-sm text-indigo-500 font-semibold">
    {children}
  </div>
);
export default TextHeadline;
